import type { DemoPackSettings } from "../types";

export const electronicsDemoSettings = {
  announcementText: "New tech arrivals and launch deals are live.",
  primaryColor: "#2563eb",
  secondaryColor: "#dbeafe",
  storeTagline: "Modern devices, smart accessories, and reliable tech essentials."
} satisfies DemoPackSettings;
